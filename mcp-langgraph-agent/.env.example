# =============================================================================
# mcp-langgraph-agent 环境变量配置模板
# 将本文件复制为 .env 并根据实际环境修改配置值
# =============================================================================

# ------------------------------------------------------------
# 应用基础配置
# ------------------------------------------------------------
APP_NAME=mcp-langgraph-agent
APP_VERSION=0.1.0
DEBUG=false

# ------------------------------------------------------------
# LLM 配置
# ------------------------------------------------------------
LLM_PROVIDER=openai
LLM_API_KEY=sk-your-api-key-here
LLM_MODEL=gpt-4o
LLM_MAX_TOKENS=4096
LLM_TEMPERATURE=0.7

# ------------------------------------------------------------
# MCP Server 配置
# ------------------------------------------------------------
MCP_SERVER_HOST=0.0.0.0
MCP_SERVER_PORT=8000
MCP_TRANSPORT=stdio

# ------------------------------------------------------------
# Appium 配置
# ------------------------------------------------------------
APPIUM_HOST=127.0.0.1
APPIUM_PORT=4723
APPIUM_BASE_PATH=/wd/hub

# ------------------------------------------------------------
# 设备池配置
# ------------------------------------------------------------
DEVICE_POOL_SIZE=2
DEVICE_CONFIG_PATH=src/config/devices.yaml

# ------------------------------------------------------------
# Redis 配置
# ------------------------------------------------------------
REDIS_HOST=127.0.0.1
REDIS_PORT=6379
REDIS_PASSWORD=

# ------------------------------------------------------------
# PostgreSQL 配置
# ------------------------------------------------------------
POSTGRES_HOST=127.0.0.1
POSTGRES_PORT=5432
POSTGRES_DB=mcp_agent
POSTGRES_USER=postgres
POSTGRES_PASSWORD=

# ------------------------------------------------------------
# Skills 配置
# ------------------------------------------------------------
SKILLS_DIR=skills

# ------------------------------------------------------------
# 日志配置
# ------------------------------------------------------------
LOG_LEVEL=INFO
LOG_FORMAT="{time:YYYY-MM-DD HH:mm:ss.SSS} | {level:<8} | {name}:{function}:{line} - {message}"